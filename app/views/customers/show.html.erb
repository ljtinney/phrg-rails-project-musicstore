<h1>Which kind of GEET are you looking to pickup today? </h1>

<%= form_for @customer do |f| %>
  <%= collection_select(:instrument, :instrument_type, Instrument.all, :id, :instrument_type) %>
  <%= f.submit "Select Instrument" %>
<% end %>
