
<h4> So you'd like to purchase a GEET? </h4>

<h5>
  <%= "To purchase #{@instrument.year_made} #{@instrument.make} #{@instrument.name} #{@instrument.model} in #{@instrument.color}" %>
</h5>

<h5> Please provide select your payment type(s)</h5>

<br>
<br>

<%= form_for([@instrument, @purchase], url: instrument_purchases_path) do |f| %>
  <input type="hidden" name="purchase[music_store_id]" value="<%= @instrument.music_store_id %>" />
  <%= f.label :payment_type %>
  <%= f.collection_select :payment_type, ["cash", "debit", "credit", "check"], :to_s, :to_s, include_blank: false %>
  <%= f.submit %>
<% end %>

<br>
<br>

<%= link_to "Log Out", logout_path %>
