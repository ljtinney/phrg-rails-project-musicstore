<h1>Purchases NEW View Test</h1>

<h4> So you'd like to purchase a GEET? </h4>
<h5> To purchase

  <%= "#{@instrument.year_made}" " #{@instrument.make}" " #{@instrument.name}" " #{@instrument.model}" " in " "#{@instrument.color}"%><h5>
  <h5> Please provide select your payment type(s)</h5>
  <br>
  <br>
  <%= form_for([@instrument, @purchase], url: instrument_purchases_path) do |f| %>
  <%= f.label :payment_type %>
  <%= f.collection_select :payment_type, PaymentType.order(:name),:id,:name, include_blank: false %>
  <%= f.submit %>
<% end %>
  <br>
  <br>
  <%= link_to "Log Out", logout_path %>
